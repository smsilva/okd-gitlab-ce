image: maven:latest

cache:
  key: "$CI_JOB_NAME"
  paths:
    - .m2/

variables:
  MAVEN_OPTS: "-Dmaven.repo.local=.m2"
  
build:
  stage: build
  script:
    - mvn compile

test:
  stage: test
  script:
    - mvn test
